# 摄入提示词 (Ingest Prompt)

请读取 `raw/` 目录下的新文档，按照以下规则处理：

1. 优先更新 Wiki 中已有的相关页面，不新建重复页面
2. 撰写摘要（不超过7句话），核心内容结构化拆分
3. 所有关键结论必须标注引用来源（格式：`../../raw/xxx/xxx.md`）
4. 不确定的内容，标记为「待补充/疑问」
5. 更新对应 Wiki 页面后，同步更新 `overview.md` 和 `links.md`
6. 生成 diff 文件放入 `diff/` 目录，等待人工审核
